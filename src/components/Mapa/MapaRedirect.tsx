import { useEffect } from "react";

const MapaRedirect = () => {
  useEffect(() => {
    window.location.href = "https://sisassistiva.cintespbr.org/";
  }, []);

  return null;
};

export default MapaRedirect;
